Python area calculator project

""" prompts the user for a shape, then calculates the area of the shape, and finally prints the shape"""

print "The calculator is starting up"
option = raw_input("Enter C for Circle or T for Triangle: ")

if option == 'C' :
  radius = float(raw_input("Enter the radius"))
  area = 3.14159 * radius**2
  print "The area of the shape is %s" % area
elif option == 'T':
 base = float(raw_input("Enter the base of the   triangle"))
 height = float(raw_input("Enter the height of the triangle"))
 area = base *0.5 * height
 print "The area of the triangle is %f" % area
else:  print "You entered an invalid shape"
print "The programme is exiting"
